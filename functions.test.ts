const {shuffleArray} = require('./utils')

describe('shuffleArray should', () => {
    // CODE HERE
    test('shuffleArray returns an array', async () => {
        expect(shuffleArray).toHaveReturned()
    })

})